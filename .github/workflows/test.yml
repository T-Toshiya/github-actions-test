name: test

on:
  workflow_dispatch:

  push:
    branches:
      - "*"

jobs:
#  notify_start:
#    uses: ./.github/workflows/notification.yml
#    with:
#      text: "Start manually deploy to dev-repository."
#      color: "#0953FF"
#    secrets:
#      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
#      - name: Post message to Slack with Payload path
#        uses: slackapi/slack-github-action@v1.19.0
#        with:
#          payload-file-path: ./.github/resources/payload-notification.json
#        env:
#          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
      - name: Post message to Slack with Payload path
        uses: slackapi/slack-github-action@v1.19.0
        with:
          channel-id: 'C034MTUB9TK'
          # For posting a simple plain text message
          slack-message: "Start manually deploy to dev-repository."
          payload-file-path: ./.github/resources/payload-notification.json
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
#  notify_end:
#    needs: notify_start
#    if: success()
#    uses: ./.github/workflows/notification.yml
#    with:
#      text: "Deployment has succeeded to dev-repository."
#      color: "#00FF00"
#    secrets:
#      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
#  notify_failed:
#    if: failure()
#    uses: ./.github/workflows/notification.yml
#    with:
#      text: "Deployment has failed to dev-repository."
#      color: "#DF0101"
#    secrets:
#      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
